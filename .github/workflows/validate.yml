name: Validate

on:
  push:
    branches:
      - test-361
  workflow_dispatch:
    

jobs:
  validate:
    runs-on: ubuntu-latest
    container:
      image: ubuntu:24.04
    steps:
      - name: Checkout sources
        uses: actions/checkout@v4
      - run: cat requirements.txt
      - uses: priya-kinthali/srcomp-validate-action@test-action-361
      - run: cat requirements.txt
